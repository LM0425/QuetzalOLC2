export enum Tipo {
    INT,
    DOUBLE,
    BOOL,
    CHAR,
    STRING,
    ARRAY,
    STRUCT,
    NULL,
    VOID
}

export enum OperadorAritmetico {
    MAS,
    MENOS,
    POR,
    DIV,
    MOD,
    UMENOS,
    CONCATENAR,
    REPETIR,
    MASMAS,
    MENOSMENOS,
}

export enum OperadorRelacional {
    MENORQUE,
    MAYORQUE,
    MENORIGUAL,
    MAYORIGUAL,
    IGUALIGUAL,
    DIFERENTE
}

export enum OperadorLogico {
    AND,
    OR,
    NOT
}