export enum Tipo {
    INT,
    DOUBLE,
    BOOL,
    CHAR,
    STRING,
    ARRAY,
    STRUCT,
    NULL,
    VOID
}

export enum OperadorAritmetico {
    MAS,
    MENOS,
    POR,
    DIV,
    MOD,
    UMENOS
}

export enum OperadorRelacional {
    MENORQUE,
    MAYORQUE,
    MENORIGUAL,
    MAYORIGUAL,
    IGUALIGUAL,
    DIFERENTE
}

export enum OperadorLogico {
    AND,
    NOT,
    OR
}